# dependencies
node_modules/
extension/node_modules/

# built artifacts
/dist/
extension/dist/
*.vsix
*.mcpb

# env / secrets
.env
.env.*
!.env.example
*.pem

# editor / OS
.DS_Store
Thumbs.db
*.log

# ephemeral local DiffGate state (learnings.json is intentionally NOT ignored — it's team-shared)
.diffgate/session.json
.diffgate/state.json

# test scratch
/tmp/

# marginal experiment output (large generated LLM outputs; reproducible via diffgate marginal)
/out/

# PR base-rate scan: harness + findings stay private (they enumerate specific
# third-party PRs); only the review-state classification script is published,
# for reproducibility of docs/posts/the-pr-was-reviewed-the-risky-line-wasnt.md
research/*
!research/review-state.mjs
.claude/settings.json
.claude/settings.local.json
.claude/output-styles/compressor-slim.md
AGENTS.md
codegraph.md
